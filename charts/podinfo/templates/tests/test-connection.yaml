apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podinfo.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ include "podinfo.fullname" . }}:9898/healthz']
  restartPolicy: Never

